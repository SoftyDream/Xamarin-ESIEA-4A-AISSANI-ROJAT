<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="XamarinApp.AddItemPage"
		     Title="Add a new task ?">
    
	<ContentPage.ToolbarItems>
		<ToolbarItem Text="Save task" Clicked="Save_Clicked" />
	</ContentPage.ToolbarItems>
    
    
	<ContentPage.Content>
		<StackLayout Spacing="15" Padding="15">
			<Label Text="Task" FontSize="Small" />
			<Entry Text="{Binding Item.Text}" FontSize="Small" />
			<Label Text="Description" FontSize="Small" />
			<Editor Text="{Binding Item.Description}" FontSize="Small" Margin="0" />
            <!--<Label Text="Date" FontSize="Small" />
            <Editor Text="{Binding Item.Date}" FontSize="Small" Margin="0" />
            <Label Text="DayTime" FontSize="Small" />
            <Editor Text="{Binding Item.Time}" FontSize="Small" Margin="0" />-->
        </StackLayout>
	</ContentPage.Content>
</ContentPage>
